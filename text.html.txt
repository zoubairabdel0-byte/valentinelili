<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Valentine 💘</title>
  <style>
    :root{
      --bg:#0b0b10;
      --card:#141421;
      --text:#f4f4ff;
      --muted:#b9b9d6;
      --green:#25c26e;
      --red:#ff4d4d;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: radial-gradient(1000px 600px at 50% -10%, #1a1a35 0%, var(--bg) 60%);
      color:var(--text);
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px;
    }
    .card{
      width:min(520px, 100%);
      background: rgba(20,20,33,.92);
      border:1px solid rgba(255,255,255,.08);
      border-radius:22px;
      overflow:hidden;
      box-shadow: 0 20px 60px rgba(0,0,0,.45);
    }
    .photo{
      width:100%;
      aspect-ratio: 9/13;
      object-fit: cover;
      display:block;
      background:#111;
    }
    .content{
      padding:18px 18px 22px;
      text-align:center;
    }
    h1{
      margin:10px 0 6px;
      font-size: 26px;
      line-height:1.15;
      letter-spacing:.2px;
    }
    p{
      margin:0 0 14px;
      color:var(--muted);
      font-size: 15px;
    }
    .buttons{
      display:flex;
      gap:12px;
      justify-content:center;
      align-items:center;
      flex-wrap:wrap;
      margin-top:10px;
    }
    button{
      border:0;
      padding:12px 18px;
      border-radius:14px;
      font-weight:700;
      cursor:pointer;
      font-size:16px;
      transition: transform .12s ease, filter .12s ease;
      user-select:none;
    }
    button:active{ transform: translateY(1px) scale(.99); }
    #yesBtn{
      background:var(--green);
      color:#062112;
      transform-origin:center;
    }
    #noBtn{
      background:var(--red);
      color:#2a0505;
      max-width: 220px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .note{
      margin-top:14px;
      min-height:24px;
      font-size:14px;
      color:#ffd7d7;
    }
    .success{
      margin-top:14px;
      font-size:18px;
      color:#bfffe0;
      font-weight:800;
      display:none;
    }
    .heart{
      display:inline-block;
      transform: translateY(1px);
    }
  </style>
</head>
<body>
  <div class="card">
    <!-- Mets l’image dans le même dossier que ce fichier et renomme-la "photo.png" -->
    <img class="photo" src="photo.png" alt="Photo" />

    <div class="content">
      <h1>Salut ma Lili d’amour <span class="heart">💘</span></h1>
      <p>Est-ce que tu veux être ma valentine ?</p>

      <div class="buttons">
        <button id="yesBtn">Oui 💚</button>
        <button id="noBtn">Non ❤️</button>
      </div>

      <div class="note" id="note"></div>
      <div class="success" id="success">Yeeees ! J’suis trop heureux 😄💐</div>
    </div>
  </div>

  <script>
    const noPhrases = [
      "Tu es sûre ? 😳",
      "S’il te plaît… 🥺",
      "Réfléchis encore 😅",
      "Allez… dis oui 🙏",
      "Promis je suis gentil 😇",
      "Non c’est interdit 😤 (jrigole)",
      "Dernière chance 😭",
      "Ok… mais… vraiment ? 😔"
    ];

    const yesBtn = document.getElementById("yesBtn");
    const noBtn  = document.getElementById("noBtn");
    const note   = document.getElementById("note");
    const success= document.getElementById("success");

    let noClicks = 0;
    let yesScale = 1;

    noBtn.addEventListener("click", () => {
      noClicks++;

      // Change le texte du bouton Non
      const phrase = noPhrases[(noClicks - 1) % noPhrases.length];
      noBtn.textContent = phrase;

      // Petit message sous les boutons
      note.textContent = "🥺 " + phrase;

      // Fait gonfler le bouton Oui
      yesScale = Math.min(2.2, yesScale + 0.15);
      yesBtn.style.transform = `scale(${yesScale})`;
    });

    yesBtn.addEventListener("click", () => {
      // Affiche le message final
      success.style.display = "block";
      note.textContent = "";

      // Bloque le "Non" après le oui (optionnel)
      noBtn.disabled = true;
      noBtn.style.filter = "grayscale(0.6)";
      noBtn.style.cursor = "not-allowed";

      // Petit effet sur Oui
      yesBtn.style.transform = "scale(1.15)";
      setTimeout(() => yesBtn.style.transform = "scale(1.05)", 140);
    });
  </script>
</body>
</html>
